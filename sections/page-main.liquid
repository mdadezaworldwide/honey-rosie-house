<div style="max-width:var(--max-w);margin:0 auto;padding:28px 20px;">
  <h1 style="font-family:'Playfair Display',serif;font-size:18px;font-weight:700;margin-bottom:20px;">{{ page.title }}</h1>
  <div style="font-size:13px;line-height:2;color:var(--text-mid);">
    {{ page.content }}
  </div>
</div>

{% schema %}
{
  "name": "Page",
  "tag": "section"
}
{% endschema %}
